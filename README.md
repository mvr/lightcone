Lightcone
=========

Lightcone is a Game of Life search tool for conduits and oscillators,
in the style of `ptbsearch`, `catalyst` and CatForce.

Usage
-----
To build, run `git submodule update --init --recursive` and then `make`.

```
./Lightcone examples/minimal.toml
```

Input Parameters
----------------

TODO: for now, see the `examples/` folder for examples, in particular
`examples/herschel.toml` for some pre-made catalyst inputs.
